import { StyleSheet } from 'react-native';

// Endast med för att hitta rätt variabler (syns inte i en .js-fil)
const styles = StyleSheet.create({
    header: {
        fontSize: 24,
        color: '#333',

    }
});